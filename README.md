## pai

### Client

```
$ poetry install
$ poetry run client
```

See `poetry run client -h` for deitals.

### Server

```
$ poetry install
$ poetry run server
```

See `poetry run server -h` for deitals.

## Poster/slide

```
$ cd poster
$ latexmk poster.tex
$ latexmk slide.tex
```
